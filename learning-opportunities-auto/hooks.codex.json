{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash|exec_command",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'base=\"${CODEX_HOME:-$HOME/.codex}/plugins/cache/learning-opportunities/learning-opportunities-auto\"; script=$(ls -1dt \"$base\"/*/hooks/post-tool-use.sh 2>/dev/null | head -1); [ -n \"$script\" ] && exec bash \"$script\" || exit 0'"
          }
        ]
      }
    ]
  }
}
